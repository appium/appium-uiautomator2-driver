name: Functional Tests

on:
  pull_request:
    branches:
    - master
    paths-ignore:
    - 'docs/**'
    - '*.md'

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        apiLevel: [36, 32, 30, 26]
        batch: [1, 2, 3, 4, 5]

    env:
      CI: true
      ANDROID_AVD: emulator
      APPIUM_TEST_SERVER_PORT: 4567
      APPIUM_TEST_SERVER_HOST: 127.0.0.1
      _FORCE_LOGS: 1
      TEST_PASS_THRESHOLD: 10
    # No hardware acceleration is available for emulators on Ubuntu:
    # https://github.com/marketplace/actions/android-emulator-runner#can-i-use-this-action-on-linux-vms
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v7
    - uses: actions/setup-node@v6
      with:
        node-version: lts/*
        check-latest: true
    - run: curl -sSL https://bit.ly/install-xq | sudo bash
    - run: |
        appium_ver=$(node -p "require('./package.json').peerDependencies?.appium")
        pushd "$(pwd)"
        cd ~
        npm install -g "appium@$appium_ver"
        popd
      name: Install Appium Server
    - run: |
        npm install
        npm install mocha-multi-reporters --save-dev
        npm install --no-save mjpeg-consumer
      name: Install dev dependencies
    - uses: actions/setup-java@v5
      with:
        distribution: 'temurin'
        java-version: '17'
    - name: Enable KVM group perms
      run: |
        echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm4all.rules
        sudo udevadm control --reload-rules
        sudo udevadm trigger --name-match=kvm
    - name: Setup Android SDK
      uses: android-actions/setup-android@v4
    - name: AVD cache
      uses: actions/cache@v5
      id: avd-cache
      with:
        path: |
          ~/.android/avd/*
          ~/.android/adb*
        key: avd-${{ matrix.apiLevel }}
    - name: Set emulator image for API level
      run: |
        case ${{ matrix.apiLevel }} in
          26)
            echo "EMU_TAG=default" >> "$GITHUB_ENV"
            echo "EMU_ARCH=x86" >> "$GITHUB_ENV"
            ;;
          30)
            echo "EMU_TAG=google_apis" >> "$GITHUB_ENV"
            echo "EMU_ARCH=x86" >> "$GITHUB_ENV"
            ;;
          *)
            echo "EMU_TAG=google_apis" >> "$GITHUB_ENV"
            echo "EMU_ARCH=x86_64" >> "$GITHUB_ENV"
            ;;
        esac
    - name: create AVD and generate snapshot for caching
      if: steps.avd-cache.outputs.cache-hit != 'true'
      uses: reactivecircus/android-emulator-runner@v2
      with:
        avd-name: ${{ env.ANDROID_AVD }}
        force-avd-creation: false
        api-level: ${{ matrix.apiLevel }}
        disable-spellchecker: true
        target: ${{ env.EMU_TAG }}
        emulator-options: -no-window -gpu swiftshader_indirect -noaudio -no-boot-anim
        arch: ${{ env.EMU_ARCH }}
        disable-animations: true
        script: echo "Generated AVD snapshot for caching."
    - run: |
        cwd=$(pwd)
        pushd "$cwd"
        cd ~
        appium driver install --source=local "$cwd"
        appium driver doctor uiautomator2
        nohup appium server \
          --port=$APPIUM_TEST_SERVER_PORT \
          --address=$APPIUM_TEST_SERVER_HOST \
          --relaxed-security \
          --log-no-colors \
          --log-timestamp \
          2>&1 > "$cwd/appium.log" &
        popd
      name: Start Appium server
    - run: nohup adb logcat > logcat.log &
      name: Capture logcat
    - uses: reactivecircus/android-emulator-runner@v2
      name: e2e_api${{ matrix.apiLevel }}_batch${{ matrix.batch }}
      env:
        E2E_BATCH: ${{ matrix.batch }}
      with:
        script: bash -xe scripts/run-functional-tests.sh
        avd-name: ${{ env.ANDROID_AVD }}
        force-avd-creation: false
        api-level: ${{ matrix.apiLevel }}
        disable-spellchecker: true
        disable-animations: true
        emulator-options: -no-snapshot-save -no-window -gpu swiftshader_indirect -noaudio -no-boot-anim
        target: ${{ env.EMU_TAG }}
        arch: ${{ env.EMU_ARCH }}
    - name: Save logcat output
      if: ${{ always() }}
      uses: actions/upload-artifact@master
      with:
        name: logcat-api${{ matrix.apiLevel }}-batch${{ matrix.batch }}
        path: logcat.log
    - name: Save server output
      if: ${{ always() }}
      uses: actions/upload-artifact@master
      with:
        name: appium-api${{ matrix.apiLevel }}-batch${{ matrix.batch }}
        path: appium.log
